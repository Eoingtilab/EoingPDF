## 2026-09-14 우클릭·기본 앱 등록 상태 확인

- 설정 화면에서 현재 사용자 영역의 PDF 열기 연결과 세 작업용 ShellBridge CLSID를 읽기 전용으로 확인합니다. 현재 설치본·다른 설치본·미등록 상태를 구분해 안내합니다.
- registry를 변경하는 동작은 기존 등록 버튼을 통해서만 수행하며, 상태 확인은 실패해도 앱을 중단하지 않습니다. 설정 smoke와 업데이트 회귀 11개 통과.

## 2026-09-14 ToUnicode 텍스트 레이어 복구

- 기존 텍스트에 U+FFFD 또는 반복된 대체 글리프가 일정 비율 이상이면 해당 텍스트 영역을 실제로 제거하고 Windows OCR 결과를 투명 텍스트로 다시 넣습니다. 정상 텍스트는 변경하지 않습니다.
- `test_tounicode_repair.py`와 기존 검색 가능 PDF 테스트 4개 통과. 원본 픽셀·원본 파일 보존과 잘못된 글리프 미검색을 확인했습니다.
- 실제 PDF 제작 도구별 ToUnicode 구조와 한글 폰트의 추가 검증은 남아 있습니다.

## 2026-09-14 스마트 다크 출력

- `smart_dark` 도구는 150DPI 페이지 이미지에서 사진·삽입 이미지 영역을 보호하고 나머지 배경·텍스트 픽셀을 반전합니다. 결과는 이미지 PDF라 텍스트 검색·양식 입력은 유지하지 않습니다.
- 회전 페이지는 화면 방향으로 렌더링하고 결과를 정방향 이미지 페이지로 저장합니다. 사진의 색상 샘플과 배경 검정 픽셀을 검사했습니다.
- `test_smart_dark.py`: 일반/회전 페이지, 사진 보존·배경 반전·원본 보존 10개 관련 테스트 통과.
- 그라데이션 배경·복잡한 투명 이미지의 의미 보존은 실문서 검증이 남아 있습니다.

## 2026-09-14 반복 이미지·로고 교체

- 문서 내 독립 raster 이미지의 해시·크기·미리보기를 목록으로 만들고, 선택한 이미지와 동일한 디코딩 픽셀을 모든 페이지에서 교체합니다. 기존 배치·회전·텍스트는 유지합니다.
- 벡터 로고·인라인 이미지·선택하지 않은 상태·문서 변경·취소는 명시적으로 차단합니다. 새 이미지 비율에 맞춘 투명 여백을 사용해 기존 위치를 유지합니다.
- `test_image_replace.py`: 동일 xref/중복 픽셀 교체, 회전 페이지·본문·원본 보존, 잘못된 선택과 취소 검사 4개 통과.

## 2026-09-14 암호화 도장·서명 보관함

- `seal_vault.py`: 이름과 배경 제거 PNG를 함께 Windows 사용자 범위 DPAPI로 암호화합니다. 최대 100개, 개별 암호화 파일 30MB 제한. UUID 파일명과 새 파일 전용 생성으로 기존 항목을 덮어쓰지 않습니다.
- `seal_vault_ui.py`: 이미지 추가·이름 입력·미리보기·선택·삭제 확인. 같은 계정의 다른 프로그램에 대한 별도 잠금은 아니며 계정/PC 이동 제약을 화면에 설명합니다.
- 날인 작업에는 암호화 파일 경로만 전달합니다. 작업 프로세스에서 복호화한 PNG는 메모리로 PDF에 넣고 평문 임시 이미지를 만들지 않습니다. 이름과 이미지의 원본 파일은 자동 삭제하지 않습니다.
- 실제 Windows DPAPI로 저장/다시 열기/손상 거부, 이름 검증, 삭제 경계, 날인 결과와 원본 보존 검사. 도장 엔진 포함 18개 테스트 통과.
- `stamp_ui_smoke.py`: 실제 보관함 UI 추가·미리보기·삭제 취소·선택 → 격리 프로세스 날인 → 300DPI PDF 저장 통과. `temp/stamp-ui-test/vault.png`로 UI 확인. 기존 UI 회귀 검사 통과.
- 다른 Windows 계정/PC에서 복호화 실패하는 실제 교차 계정 검사는 아직 수행하지 않았습니다. 이 기능은 현재 소스에만 반영했으며 설치파일 재빌드가 남아 있습니다.

## 2026-09-14 입력 범위 방어 보강

- 도장 날인 페이지 입력을 비워 두면 안내문대로 전체 페이지를 선택합니다. 잘못된 범위는 결과를 발행하지 않습니다.
- 이미지 일괄 교체는 원본 이미지 선택 없이 저장할 수 없도록 UI에서 차단합니다.
- 관련 범위·교체·날인 테스트 17개 통과.

## 2026-09-14 도장·서명 흰 배경 제거 및 날인

- PNG/JPEG에 EXIF 방향을 반영하고 흰 배경을 투명화합니다. 기존 알파를 유지하며 외곽 빈 영역을 자릅니다. 20MB/4천만 픽셀 제한과 빈 이미지 거부를 적용했습니다.
- 지정 페이지 범위와 화면 기준 mm 위치/너비로 날인합니다. 네 방향 회전 및 기존 CropBox를 유지합니다. 범위를 벗어난 배치는 결과를 발행하지 않습니다.
- 선택적으로 전체 문서를 300DPI PNG 기반 이미지 PDF로 합칩니다. 선택 해제 시 원문 텍스트/벡터를 유지합니다. 전체 합치기는 텍스트/양식/링크를 잃는다는 설명을 UI/README에 포함합니다.
- `test_stamping.py`와 기존 워터마크 테스트: 18개 통과. 배경 픽셀 보존·다른 페이지 보존·정확한 이미지 해상도·원본 해시·알파·잘못된 값·취소/임시파일 정리 검사.
- `stamp_ui_smoke.py`: 실제 UI→별도 변환 프로세스→PDF 저장 경로 통과. 도구별 옵션 표시와 완료 후 비교 버튼 확인. `temp/stamp-ui-test/dialog.png`, `result.png`를 직접 시각 확인했습니다.
- 전체 자동 테스트 193개 통과(12.36초). 기존 UI 회귀 테스트 통과. 도장 smoke를 배포 빌드 게이트에 추가했습니다. 현재 설치파일에는 아직 반영하지 않았습니다.
- 실물 서명 사진의 다양한 조명 품질, 암호화 서명 보관함, 전체 명세 테스트는 남아 있습니다.

## 2026-09-14 암호 PDF 뷰어 직접 열기

- 열기 암호를 마스킹 입력하고 잘못된 암호는 재입력합니다. 취소하면 기존 문서를 유지합니다.
- 암호는 세션 메모리로만 미리보기·페이지 복사·슬라이드쇼·발표자·양식 작업에 전달합니다. 열기용 복호화 임시 PDF를 만들지 않습니다.
- 페이지 삭제 저장은 원본 암호화를 유지한 새 사본이며, 저장 후 페이지 수를 재검증합니다. 양식 입력 도구의 결과는 기존 정책대로 암호 없는 사본이고 화면에 명시합니다.
- `tests/encrypted_viewer_smoke.py`: 잘못된 암호 후 성공, 렌더·썸네일·발표자, 중간 삭제, AES-256 유지/암호 인증, 원본 바이트 보존, 취소 상태 보존 통과.
- 현재 제품 테스트 `pytest tests` 181개 통과(16.95초). 최초 무경로 실행은 과거 백업 테스트 8개 수집 오류가 있어 pytest.ini의 기본 testpaths를 tests로 지정했습니다.
- 기존 UI 회귀·발표자·양식 입력 smoke 모두 통과. 빌드 게이트에 암호 뷰어 검사를 추가했습니다.
- 이 변경은 현재 소스에만 반영되어 있으며 설치파일 재빌드는 남아 있습니다.

# 2026-09-14 자동 업데이트 백업·롤백

- 업데이트 설치 직전에 실행 중인 `EoingPDF.exe`의 SHA-256과 버전을 `%LOCALAPPDATA%/EoingPDF/updates/backups`에 저장합니다. 최근 3개만 유지하고 파일명은 덮어쓰지 않습니다.
- `--rollback-update`는 설치 경로가 백업 매니페스트와 일치하고 백업 파일의 `MZ` 헤더·크기·SHA-256이 맞을 때만 원자적으로 실행 파일을 복원합니다. 손상·다른 설치 경로의 백업은 건너뜁니다.
- `tests/test_updates.py`: 정상 백업 후 새 실행 파일을 이전 파일로 복원하는 검사와 조작된 백업 거부 검사를 통과했습니다. 자동 설치 호출도 백업 경로를 전달하는지 확인했습니다.
- 실제 Inno Setup 설치를 강제로 실패시킨 뒤 복원하는 검증과 코드 서명은 아직 남아 있습니다.

# 2026-09-14 문서 소독 보강

- 메타데이터 도구가 작성자·생성기·XMP를 제거하는 기존 동작에 더해, PDF 텍스트 span의 제로폭 유니코드 문자(`U+200B/U+200C/U+200D/U+2060/U+FEFF`)를 실제 redaction으로 지웁니다.
- 12×12pt 이하의 노란 채움 그래픽은 투명 redaction으로 PDF 그래픽 객체에서 제거합니다. 큰 노란 도형·이미지의 의미 있는 내용은 자동 삭제하지 않습니다.
- `tests/test_sanitize.py`: 본문 보존, 제로폭 문자 제거, 작은 추적점 제거, 회전과 무관한 원본 보존, 취소 시 결과 미발행 검사 통과.
- 이 기능은 문서 전체의 모든 포렌식 흔적 제거를 보장하지 않으며, 그림자·이미지 내부 MIC 패턴은 별도 분석이 필요합니다.

# 2026-09-14 우클릭 작업 목록 방어

- ShellBridge가 생성한 `%LOCALAPPDATA%/EoingPDF/queue/*.files`만 읽고, UTF-8 BOM·한글 경로·빈 줄을 처리합니다.
- 목록은 2MB·1000개 경로로 제한하고 모든 항목이 절대 경로의 실제 파일인지 확인합니다. 다른 폴더의 목록이나 없는 파일은 거부하며, 처리가 끝나거나 실패하면 우리 큐의 목록만 삭제합니다.
- `test_quick_manifest.py`의 정상 한글 경로·잘못된 경로·소유 폴더 검사 통과. 전체 테스트 210개 통과.

# 2026-09-14 자동 진단 D-10 연결

- 백그라운드 진단이 암호 PDF를 열기 전에 감지해 `D-10 / 암호 PDF · 암호 입력 필요 / decrypt` 칩을 반환합니다. 암호를 저장하거나 추측하지 않습니다.
- 읽을 수 없는 PDF는 `D-10 / 손상 가능성 · PDF 복구 확인 / repair`로 분류합니다. 기존 일반 PDF 진단은 회귀 없이 유지합니다.
- `test_sniffer_extended.py` 및 기존 진단 테스트 5개 통과. 새 프로세스 포함 30ms 목표는 문서에 기록된 대로 여전히 미달 가능성이 있습니다.

# 2026-09-14 이미지 캡처 자동 진단

- 첫 페이지에 실제 이미지가 2개 이상 있으면 `D-08 / 캡처 이미지 세로 연결 / stitch`를 자동 제안합니다. 기존 호출 기반 캡처 제안도 유지합니다.
- 진단 칩에서 세로 연결 도구로 바로 이동하며, 일반 문서·암호·손상 진단과 회귀하지 않습니다. 관련 진단 테스트 6개 통과.

# 2026-09-14 설정 화면의 업데이트 복구 안내

- 설정 → 업데이트 탭에서 검증된 백업 버전의 존재와 버전을 표시합니다.
- 복원 안내 버튼은 실행 중인 파일을 덮어쓰지 않고, 앱을 완전히 종료한 뒤 `EoingPDF.exe --rollback-update`를 실행하도록 안내합니다. 복원 명령은 경로·해시 검증 후 원자적으로 교체합니다.
- `test_settings_rollback.py`와 업데이트 회귀 테스트 11개 통과.

# 정식 배포 검증 기록

## 2026-09-14 Bates 번호·목차 표지

- 병합 결과에 원본 파일별 시작 페이지 책갈피 생성. 보조 도구에서 접두어/시작 번호/자리 수 지정, 기존 하단 숫자 가리기 및 한 장 클릭 목차 표지 선택.
- 회전/CropBox 유지하며 화면 기준 하단 중앙에 연속 번호 표시. 목차 표지 추가 시 책갈피와 클릭 링크의 목적지 보정.
- 전용 7개와 기존 엔진 포함 21개, UI 회귀 검사 통과. 병합 원본별 링크, 4방향 번호 위치, 기존 숫자 redaction 후 검색 불가 및 본문 보존, 잘못된 입력 미게시 검사.
- 기존 번호 탐지는 하단 10% 영역의 단순 숫자/숫자 분수/Page 형식으로 제한. 이미지 속 숫자·복잡한 머리말은 자동 삭제하지 않으며, 목차 표지는 최대 40개 항목입니다. 실제 복잡한 실문서와 더 긴 목차의 사용성 검증은 남음.

## 2026-09-14 내장 양식 입력

- 뷰어의 양식 입력 버튼에서 현재 페이지의 텍스트/체크 필드를 편집하고 새 PDF로 저장 후 열기. 페이지 삭제 변경이 있으면 먼저 별도 저장하도록 연결.
- 읽기 전용 필드 비활성화, 비밀번호 필드 마스킹, 다중 행 텍스트 편집기, 값 타입/길이/알 수 없는 필드 검증. 값은 작업 프로세스 stdin으로 전달하며 명령행/로그에 포함하지 않음.
- 양식 엔진 7개 검사, 실제 Qt 입력→작업 프로세스→저장 UI 검사, 기존 뷰어 회귀 통과. 한글 ‘홍길동’과 체크 상태를 다시 열어 확인하고 렌더링에서도 표시 확인. 원본 보존.
- 선택 목록/라디오 필드 편집과 외부 뷰어별 한글 재편집 호환성, 생성 후보 검토 UI는 남음. 최신 통합 설치파일 이후의 소스 변경임.

## 2026-09-14 자동 PDF 양식 생성 기반

- 비어 있는 벡터 사각형/밑줄, 텍스트 밑줄/대괄호 체크 칸을 실제 TextField/Checkbox로 생성. 글자가 이미 있는 칸과 기존 필드 영역은 제외하고 이름 충돌 방지.
- 2,000개 신규 필드 제한, 감지 없음/취소 시 결과 미게시. 원본 바이트 보존.
- 전용 6개와 기존 보조 도구 포함 14개 검사 및 UI 검사 통과. 4방향 회전, ASCII 값·체크 상태 입력 후 저장, 재처리 중복 생성 없음, 대괄호 감지 확인.
- 내장 뷰어에는 직접 폼 입력 UI가 아직 없으며 현재 결과는 양식 지원 뷰어에서 입력해야 함. 한글 입력 표시, 스캔/복잡한 표 인식, 사용자의 감지 후보 검토는 후속 요구. I-01 전체 사용 흐름 완료는 아님.

## 2026-09-14 세로 연결 및 A4 분할

- 같은 너비로 비율 조정 후 페이지 사이 추가 간격 없이 벡터 연결. 표시 회전/CropBox/주석 유지. 원래 페이지 내부 여백은 유지하며 전체 높이 14,400포인트 제한을 초과하면 저장하지 않음.
- A4 분할은 축소 렌더링의 연속된 밝은 가로 구간을 탐색하고 텍스트 줄/일반 크기 이미지 내부는 보호. 분할점 없는 큰 구간은 한 장에 축소해 보존.
- 전용 7개 및 인쇄/좌우 분리 회귀 포함 24개 검사, UI 검사 통과. 연결 픽셀 비교·순서/원본 보존, 혼합 너비, 높이 제한, 문단 비절단/중복 없음, 분할 불가 도형 보존 확인.
- 실제 웹툰·다단·스캔 모음의 분할 품질은 미검증. 원래 여백 자동 제거와 모든 문맥 보존 전체 요구가 완료된 것은 아님.
- 이 변경은 앞서 만든 2.2.0 통합 설치파일 이후의 소스 변경이며 다음 패키징에 포함해야 함.

## 2026-09-14 통합 실행파일 및 설치파일

- 전체 pytest 160개 및 배포 전 UI 검사 통과. 실제 EXE에서 SVG·Markdown·워터마크·기울기 보정·검색 OCR·소책자 출력 6개 검증.
- 포터블/설치형/소스 빌드 생성. 파일과 해시는 `INTEGRATION_BUILD_2.2.0.md` 참조.
- 최신 설치파일의 설치/제거·실서버 업데이트는 미실시. 정식 전체 완료는 아님.

## 2026-09-14 SVG 내보내기

- 페이지별 SVG를 고정 순번 파일명으로 ZIP 저장. 텍스트는 윤곽선, 이미지는 data URI로 포함하고 표시 주석/폼을 내용에 반영.
- 전용 6개 검사 및 기존 분할 검사 포함 총 14개 통과. UI 저장 종류/옵션 표시 검사 통과.
- 네 방향 회전·CropBox·주석 포함 SVG를 재렌더링해 원본과 크기 일치 및 평균 채널 픽셀 오차 1 미만 확인. SVG text 요소 미존재·path 요소 존재, 이미지 외부 파일 의존 없음 검사.
- 원본 보존·기존 결과 보호·취소 임시 파일 정리 확인. DXF 변환이나 PDF 자체의 글꼴 윤곽선화 완료를 의미하지 않음. 외부 편집기 및 복잡한 실문서 호환성 검증은 남음.

## 2026-09-14 OCR·워터마크 회전/CropBox 보존

- OCR와 문구/PNG 워터마크에서 remove_rotation 호출 제거. 표시 좌표를 역회전해 원래 PDF 좌표로 변환하며 원래 회전 및 CropBox 유지.
- PNG 삽입 시 회전 페이지에서 중심이 어긋나는 문제를 재현하고, 삽입 구간에서만 회전을 0으로 설정한 후 finally로 복원하도록 수정.
- 4방향 × OCR/문구/PNG 12조합 및 기존 검사 총 21개 통과. OCR 추가 전후 픽셀 동일, 인식 텍스트 표시 좌표 1포인트 이내, 숨겨진 여백 미노출, 워터마크 중앙·대각선 및 원본 바이트 보존 확인.
- OCR 좌표 회귀는 합성 응답을 사용하고 기존 실제 Windows OCR 검사도 통과. 다양한 실제 스캔 인식률이나 전체 문서 보존을 보증하지 않음.

## 2026-09-14 좌우 분리 및 인쇄 CropBox 보존

- 좌우 분리에서 표시 주석·폼을 내용에 포함하고, 보이는 CropBox를 벡터 상태로 정규화한 다음 왼쪽/오른쪽으로 분리.
- 설치된 MuPDF의 remove_rotation이 회전된 비기본 CropBox를 풀어 버리는 현상을 재현. 좌우 분리와 인쇄 배치에서 해당 호출을 제거하고 표시 영역과 회전각을 명시적으로 전달.
- 0/90/180/270도 × 기본/사용자 CropBox 8조합에서 분리 후 픽셀을 다시 합쳐 원본과 비교. 평균 채널 오차 0.5 미만, 20 초과 차이 픽셀 비율 1% 미만. 원본 바이트·텍스트·벡터 보존 확인.
- 빈 페이지·홀수 폭, 인쇄 배치의 숨겨진 빨간 여백 비노출 검사 포함 관련 25개 테스트 통과.
- 원본의 링크/목차 상호작용을 분리 결과에 재매핑하는 기능은 포함하지 않음. 실제 복잡한 스캔 모음의 추가 검증은 남아 있음.

## 2026-09-14 회전 PDF 여백 자르기

- 90/180/270도 페이지를 건너뛰던 제한 제거. 비회전 내용 좌표와 CropBox 크기로 경계를 계산하고 PDF 회전 정보 유지.
- 링크 사각형은 화면 회전 좌표로 반환됨을 실제 저장/재오픈으로 확인하고 역회전해 보존 경계에 포함. 본문 밖 링크가 잘리는 문제 방지.
- 회전/기존 CropBox 8개 및 링크 4개 조합, 기존 보조 도구 검사 포함 총 20개 통과.
- 텍스트·방향·벡터·주석·렌더링 색상·링크 목적지·원본 바이트 보존 확인. 입력이 복잡한 실문서 전 범위를 검증했다는 의미는 아님.

## 2026-09-14 라이선스 다국어

- 라이선스 화면 제목·키 안내·접근성 이름·활성화/비활성화·상태·오류 안내를 세 언어 리소스에 연결. 설치 과정의 라이선스 실행도 OS 언어를 선택.
- `license_ui_smoke.py`: 한국어/영어/일본어, 비밀번호 마스킹, 미인증 계속 차단, 오류 표시 검사 통과. 실제 키/서버 대신 빈 모의 저장소만 사용.
- 라이선스/리소스 관련 9개 검사 통과. 영문 렌더링에서 버튼과 줄바꿈 확인. 배포 전 필수 UI 검사에 포함.
- 원본 JSON 키 일치 검사로 번역 누락을 검출하도록 보강. 메인/뷰어/보조 도구 전체 번역은 남아 있음.

## 2026-09-14 다국어 기반 및 설정 화면

- `assets/locales/ko-KR.json`, `en-US.json`, `ja-JP.json` 추가. Windows GetUserDefaultUILanguage 기반 선택, 미지원 언어/리소스 누락/손상 시 한국어 또는 원문 사용.
- 앱 시작 시 OS 언어를 선택하고 Qt 기본 대화상자 번역 적용. 기존 한국어 테스트 진입점은 유지.
- 설정 탭/설명/버튼/업데이트 상태 번역 적용. PyInstaller assets 수집 및 소스 패키지 목록에 리소스 포함.
- 로더·Windows 언어 선택·폴백 3개 검사, 한국어 UI 회귀 검사, 영어/일본어 설정 화면 표시 검사 통과. 일본어 캡처에서 글자 표시 확인.
- 메인 작업 화면·보조 도구·라이선스 내부 화면·뷰어 등의 전체 번역은 남아 있음. 전체 앱 다국어 완료로 표시하지 않음.

## 2026-09-14 업데이트 무결성 검증 강화

- 새 버전의 SHA-256은 64자리 16진수 필수. 누락/잘못된 값은 다운로드 전에 거부.
- 다운로드 함수에서도 HTTPS·app.nal.la·자격 증명/포트/제어문자/fragment 조건을 재검사. 다운로드 후 및 설치 실행 직전에 파일 헤더·크기·SHA-256 검증.
- 업데이트 관련 8개 테스트 통과: 정상 다운로드, 해시 불일치·잘못된 응답·임시 파일 정리, 작업 중 설치 보류, 버전 비교, 허용 서버, 해시 누락, 다운로드 직접 호출 검증, 설치 대기 중 파일 변조 시 실행/앱 종료 차단.
- 설정 UI 검사 통과. 실서버 설치파일/해시 등록 및 실제 업데이트 설치는 미검증. 같은 응답의 해시는 코드 서명이나 서버 침해 방어를 대체하지 않으며 백업/롤백도 별도 남은 요구임.

## 2026-09-14 통합 설정 화면

- 메인 앱의 개별 설정 버튼을 일반/라이선스/업데이트 탭 한 곳으로 통합. 공통 SDK 토큰 적용.
- `settings_ui_smoke.py`에서 셸/라이선스 호출 전달, 업데이트 미시작/확인 중/설치 대기 상태, 중복 요청 비활성화, 종료 시 상태 타이머 정리 확인. 실제 키·라이선스 서버는 접근하지 않은 UI 검사.
- 기존 `ui_regressions.py` 통과. 설정 화면 캡처에서 상태 문구와 탭·버튼 표시 확인. 배포 전 필수 UI 검사에 추가.
- SDK WPF 전체 통합·로그 센터·업데이트 체크섬/롤백·실서버 업데이트 검증은 여전히 남아 있음.

## 2026-09-14 인쇄 배치

- A4 가로 4쪽 배치, 소책자 양면 순서 배치 추가. 원본은 유지하고 주석/폼의 표시를 결과 내용에 포함.
- 전용 5개 검사: 1~100쪽 소책자 순서·4의 배수가 아닌 페이지 수, 결과 텍스트·벡터/페이지 범위, 회전 및 주석, 취소 시 출력 미잔존.
- 전체 pytest 112개, UI 회귀 검사 통과. 가로 발표 문서 D-04에 4쪽 모아찍기 바로가기 연결.
- 합성 8페이지의 실제 렌더링에서 4쪽 배치 1/2/3/4 및 소책자 앞면 8/1 순서·여백 확인. 증적: `temp/imposition-preview/`.
- 저장 PDF를 인쇄하는 기능이며 직접 프린터 전송은 아님. 소책자 짧은 쪽 넘김/실제 크기 인쇄 안내 포함. 실제 프린터 양면 방향·접기 검증은 미실시.

## 2026-09-14 Markdown 추출

- 보조 도구에서 `.md` UTF-8 새 파일로 저장. 페이지별 처리, 원본 페이지 주석, 글꼴 크기 기반 제목·문단·격자 표 추출.
- 추출된 표 영역은 본문에서 중복 제외. Markdown/HTML 특수문자 이스케이프. 한글 폰트 추출에서 나타난 NBSP를 일반 공백으로 정규화.
- 텍스트 없는 페이지와 포함하지 않은 이미지를 결과에 명시. 자동 OCR이나 외부 전송 없음.
- Markdown 4개 + 보조 도구/프로세스 관련 총 17개 검사 통과. UI 회귀 검사 통과.
- 검사 범위: 제목/본문/2열 표, 원본 보존, 한글·AES-256 인증, 잘못된 암호, 취소·기존 결과 보호, 저장 UI.
- 복잡한 다단 읽기 순서, 병합 셀, 의미 기반 제목 계층의 완전 보존은 미검증. I-06의 구조 완전 보존 전체 요구를 충족했다고 주장하지 않음.

## 2026-09-14 교차 참조 복구

- `recovery.py`: 읽을 수 있는 PDF 객체에서 교차 참조 구조를 재작성. 저장 사본이 추가 자동 복구 없이 열리는지, 페이지 수·영역·텍스트 SHA-256·최대 768픽셀 렌더링 SHA-256이 일치하는지 검사 후 원자적으로 게시.
- 복구 전용 6개 검사 통과: startxref 오프셋 손상, xref 표 삭제, xref 이후 꼬리 유실, 복구 불가 바이너리/기존 출력 보호, 검증 도중 취소, AES-256 암호 인증.
- 합성 입력에서 목차 및 원본 바이트 보존 확인. 임시 파일과 실패 결과 미잔존 확인.
- 전체 pytest 103개 및 UI 회귀 검사 통과. XML: `temp/test-results/pytest.xml`.
- 이미 유실된 페이지/본문 객체를 생성하지 않으며, 렌더링 검사는 복구 엔진이 현재 읽을 수 있는 내용을 기준으로 함. 완전한 원문 복원이나 모든 객체 무결성을 보장하지 않음. 실제 손상 문서 모음 및 배포 EXE 검증은 남음.

## 2026-09-14 스포트라이트·레이저

- S 스포트라이트, Ctrl+L 레이저 토글 및 최대 256점/0.5초 잔상 구현. 잔상이 없으면 타이머 종료. 창 숨김/종료 시 타이머와 점 제거.
- 발표자 현재 미리보기의 마우스 좌표를 여백과 픽셀 배율을 반영해 청중 캔버스에 전달.
- `presenter_ui_smoke.py`: 밝은 스포트라이트 중심·어두운 주변, 레이저 빨간 픽셀, 600ms 후 점/타이머 제거, 창 종료 중 타이머 정리 검사 통과.
- 테스트의 물리 커서 이동만으로는 이벤트 전달이 안정적이지 않아 Qt MouseMove 이벤트를 대상 위젯에 직접 전달해 경로 검증. 실제 듀얼 모니터의 물리 마우스 조작은 아직 미검증.
- `viewer_smoke.py` 회귀 검사 통과. 현재 Qt 캔버스 효과이며 Direct2D 투명 판서나 120fps 요구를 충족했다는 의미는 아님.

## 2026-09-14 발표 그리드·커튼

- G 키 그리드에서 현재 편집 페이지 순서를 사용하고 삭제 예정 페이지를 제외. 클릭 이동, Esc 취소, 발표 종료 시 목록/타이머 정리 검사 통과.
- 합성 200페이지 PDF에서 처음/마지막 구간 스크롤 시 이전 구간 아이콘 해제 확인. 한 이벤트당 최대 2개 렌더링하며 화면 밖 픽셀 캐시를 해제. 복잡한 실문서 렌더링 지연은 별도 측정 필요.
- Space는 텍스트 블록이 여러 개면 위에서부터 공개, 모두 공개한 다음에는 다음 페이지로 이동. 한 블록 또는 스캔 문서는 바로 다음 페이지. 방향키/클릭은 즉시 페이지 이동.
- 실제 Qt 캡처에서 커튼 검정 픽셀, 흰 화면 우선 표시, 다음 페이지의 커튼 초기화 검사 통과.
- `presenter_ui_smoke.py`, 기존 `viewer_smoke.py` 통과. 다단 문서 의미상 읽기 순서와 스캔 OCR 기반 커튼은 아직 미검증/미구현.

## 2026-09-14 발표자 화면 기반

- 현재/다음 페이지 미리보기, 스티커 메모 대본, 경과 시간, 예정 시간 대비 페이지 진도 경고 구현.
- 보조 모니터가 있으면 청중 전체화면과 발표자 창을 서로 다른 화면에 배치. 실제 듀얼 4K 장비 검증은 미실시.
- `presenter_ui_smoke.py` 통과: 삭제 예정 페이지 제외, 대본 전환, 마지막 페이지, B/W 실제 픽셀, 두 창 동시 종료·타이머 중단, 원본 해시 보존.
- 기존 `viewer_smoke.py` 통과. 발표자 검사도 배포 전 필수 검사에 추가.
- 렌더링 이미지에서 청중용 어두운 스타일이 HUD 글자 배경에 상속되는 문제를 확인하고 HUD 스타일을 분리.
- Direct2D 판서·전체 주석 대본·커튼 노출·그리드 등 확장 요구는 아직 미구현.

## 2026-09-14 목표 용량 압축

- 전체 pytest 97개 통과. 결과: `temp/test-results/pytest.xml`. 전체 MASTER SPEC 완성을 의미하지 않음.
- 압축 전용 9개: 목표 크기 이하 저장, 텍스트/벡터·원본 보존, 공유 이미지 최대 사용 크기 보존, 목표 미달 및 취소 시 출력/임시 파일 제거, 잘못된 입력 거부.
- API 해상도 조건 오류와 실제 이미지 해상도가 줄지 않는 문제를 재현하고 수정. 각 후보는 원본의 최적화 사본에서 독립 생성.
- UI 회귀 검사 통과: 10/25MB 선택, 직접 변경 시 선택 표시 동기화, 기존 뷰어 너비 맞춤·페이지 이동·한글 버튼 유지.
- 이미지는 손실 압축될 수 있음. 72DPI/품질 40까지 측정하며 도달 불가능한 목표는 실패로 안내. 투명/스텐실 이미지의 직접 리사이즈는 보존을 위해 제외.
- 배포 EXE 및 다양한 실제 문서의 시각 품질 검증은 남아 있음.

## 2026-09-14 두 문서 비교

- 동일 문서 차이 없음·원본 보존, 삭제/추가 색상, 회전/페이지 수 차이, 암호 인증 검사 통과.
- 비교 엔진과 기존 프로세스 통합 관련 테스트 총 9개 통과.
- `diff_ui_smoke.py`에서 실제 Qt 렌더링 픽셀로 차이 색상·좌우 슬라이더 양 끝·겹침 양 끝·누락 페이지·파일 오류 시 이전 이미지 제거를 확인.
- 보조 도구의 PDF 결과에서 원본과 비교 버튼 연결. 암호는 메모리에서만 사용하고 비교창 종료 시 입력 필드 제거.
- 비교는 같은 페이지 번호와 물리적 배율 기준이며 의미 분석이나 자동 문서 정렬은 수행하지 않음. 정식 전체 완료는 아님.

## 페이지·표 클립보드 확장

- CF_DIB 300DPI/24비트 헤더, 한글 CF_HTML 바이트 오프셋, HTML 이스케이프, TSV, 수식 텍스트 처리, 0/90/180/270도 선택 영역 8개 검사 통과.
- `clipboard_ui_smoke.py`: 실제 Qt 드래그 → 표 추출 → 복사 함수 전달 및 이미지 복사 버튼 검사 통과. OS 클립보드 게시만 시험용 함수로 대체하여 기존 클립보드는 변경하지 않음.
- 실제 Windows/Excel 붙여넣기 상호운용은 아직 미검증.
- 복사 안내가 재렌더링으로 사라지는 결함을 재현한 뒤 5초 유지하도록 수정. 동일 드래그 회귀 검사 통과.
- 기존 뷰어/삭제/슬라이드쇼 smoke 검사 통과.

## 범위·묶음·목차 분할 확장

- pytest 전체 **76개 통과**, UI 회귀 검사 통과.
- 범위별 ZIP 출력: `1-3, 5, 8-end`, 역순 범위 검증.
- 일정 페이지 수 분할의 짧은 마지막 묶음 보존 검증.
- 최상위 목차 분할의 앞부분 및 전체 페이지 보존, 목차 제목의 경로 문자 제거 검증.
- 잘못된 범위 및 취소 시 출력·임시 파일 미잔존 검증.
- UI의 분할 범위/묶음 크기 전환과 ZIP 저장 표시 확인.
- 전체 명세 구현 및 정식 배포 게이트는 아직 미완료.

## 워터마크 확장 회귀

- 문구 45도 방향, 회전된 페이지, 원본문구 보존, PNG 투명도, 잘못된 불투명도 입력 6개 검사 통과.
- 워터마크·기존 보조 도구·프로세스 통합 검사 총 19개 통과.
- UI 회귀 검사에서 문구/PNG 옵션 전환과 필드 표시 확인.
- 보조 도구 드롭다운과 불투명도 입력의 밝은 배경/글자 대비를 명시적으로 적용.
- 정식 완료 게이트는 아직 미통과이며 전체 남은 기능은 `EXTENSION_STATUS.md`에서 유지.

## 2026-09-13 OCR·진단·프로세스 격리 확장

- pytest 전체 **62개 통과**. JUnit: `temp/test-results/pytest.xml`.
- UI 회귀, 뷰어/슬라이드쇼, 메인/빠른 병합 smoke script 모두 통과.
- OpenCV 합성 문서 기울기와 저대비 보정, 300DPI 이미지 출력, 메타데이터 제거 검사 포함.
- 실제 Windows OCR 단어 좌표 및 검색 가능한 PDF 확인. 투명 레이어 픽셀 보존·좌표 정합 검사 통과.
- 스캔 개인정보는 합성 문서와 주입한 OCR 좌표로 이미지/텍스트 삭제 검사. 다양한 실문서 OCR 정확도와 탐지 재현율 검증은 남음.
- 비밀 암호는 변환 프로세스 stdin으로 전달. Windows Job의 자손 종료 및 실제 OCR 작업 임시 폴더 제거 검사 통과.
- MicroSniffer 첫 페이지 엔진 중앙값 2.189ms, 프로세스 시작 포함 중앙값 270.019ms. **전체 30ms 목표 미달**.
- 정식 버전 전체 완료 아님. 남은 요구와 품질 계획: `EXTENSION_STATUS.md`, `QUALITY_PLAN.md`.

## 2026-09-13 확장 소스 검증

- `python -m unittest discover -s tests -v`: 33개 통과.
- `tests/ui_regressions.py`: 기존 뷰어 회귀 + 보조 도구 옵션 전환 통과.
- 암호 설정/해제, 권한, 원본 보존, 취소 임시 파일 정리, 개인정보 텍스트 스트림 삭제 검증.
- 오프라인 재실행 허용 및 명시적 서버 취소의 영구 반영 검증.
- LibreOffice는 모의 프로세스 검증만 통과; 실제 프로그램 미설치로 실변환 미검증.
- SDK는 원본 테마 토큰 연동 단계. WPF 공통 화면 전체 통합 및 전체 DPI 검증 미완료.
- MASTER SPEC 전체/ISO 전체 품질 게이트/최종 설치파일 검증 완료를 의미하지 않음.

## 2.2.0

- 23개 엔진/라이선스/업데이트 단위 테스트 통과.
- UI 회귀 테스트: 최초 페이지와 다음 페이지 너비 일치, 창 변경 후 너비 재계산, 페이지 ▲/▼ 동작, 한국어 저장/버리기/취소, 작은 창의 파일 선택 버튼 높이 확인.
- 실제 app.nal.la 상품 26818이 어잉PDF임을 확인. 사용자가 직접 입력한 키로 상태 확인 → 비활성화 → 재활성화 → 상태 확인 통과. 마지막에 활성 상태 복원.
- 서버 `get_version` 응답의 버전은 아직 빈 값. 자동 업데이트 다운로드/설치 제어는 격리 테스트로 확인했으며, 운영 서버에서 새 버전 파일을 제공하는 전체 과정은 아직 검증하지 못함.
- 라이선스 키와 기기 식별자는 테스트 출력/배포에 포함하지 않음.

아래 결과는 2026-09-12 현재 직접 수행한 검증만 기록한다. 배포 대상은 사용자의 현재 Windows PC와 비공개 저장소다.

## 통과

- 2.1 뷰어 검사: 미리보기 클릭, X 삭제 확인/취소, 저장 전 원본 보존, 저장 취소, 미저장 닫기 취소, 최종 PDF 내용 검증 통과.
- 2.1 슬라이드쇼 검사: 현재 페이지에서 전체화면 시작, Space/방향키/좌우 마우스/Home/End, 끝 페이지 경계, Esc 후 뷰어 페이지 동기화 및 삭제된 페이지 제외 통과.

- `tests/viewer_smoke.py`: 독립 뷰어 렌더링·페이지 이동·150% 확대 및 UI의 중간 페이지 삭제. 3페이지 중 2번 삭제 후 1·3번 유지, 원본 SHA 동일, 전체 삭제 거부.
- Windows 기본 앱 설정에서 어잉PDF 항목 및 `.pdf` 연결 선택을 확인. 현재 사용자 `FileExts/.pdf/UserChoice`의 ProgId가 `EoingPDF.Document`인 것을 확인했다.
- 로그 파일 접근이 거부되는 환경에서도 임시 로그/무로그로 시작하도록 수정. 동일 환경에서 실제 `main.py PDF --screenshot` 뷰어 시작과 정상 종료를 확인했다.

- `python -m unittest discover -s tests -v`: 14개 통과.
  - 페이지 추출 순서/중복, 잘못된 범위와 과도한 숫자, 선택 페이지 회전
  - 이름 충돌 시 기존 출력 보존, 취소 시 임시 파일 정리, 암호 PDF 거부
  - PNG ZIP/텍스트 추출, 페이지 번호, CP949 한글 입력
  - PDF/PNG/TXT 혼합 병합, 여러 프레임 TIFF
  - 요약 출처 페이지, 부분 실패 명시, 모든 테스트의 입력 SHA-256 보존
- `tests/ui_smoke.py`: 편집창 버튼 → 작업 스레드 → 출력, 빠른 병합창 → 2페이지 출력.
- `tests/native_smoke.py word excel powerpoint`: 새로 만든 DOCX/XLSX/PPTX를 실제 Office로 PDF 변환하고 텍스트 및 페이지 확인.
  - 첫 측정은 테스트 문서 생성 시간을 포함하여 Word 9.58초, Excel 5.69초, PowerPoint 3.36초. 변환 단독 성능으로 해석하면 안 된다.
- 포터블 EXE에서 `--screenshot` 정상 종료 및 화면 생성.
- 우클릭 연결 프로그램의 CF_HDROP 자체 검사: 여러 파일과 한글/공백/특수문자 경로 보존.
- COM 서버를 직접 시작한 통합 검사: 전체 선택 전달 → 포터블 앱 빠른 병합 → 실제 2페이지 출력 확인.
- **탐색기 실제 우클릭**: 앱에서 메뉴 등록 후 두 PDF를 선택하여 병합, 2페이지와 본문 순서를 확인했다. 메인 창 없이 빠른 작업창이 자동 실행됐다.
- HWP/HWPX: 실제 사용자 탐색기에서 실행한 검사로 한글 2022 변환 성공. 각각 4.62초/4.52초(테스트 문서 생성 포함). 접근 DLL 해제 시 정리 오류를 수정했다.
- DOC/RTF/XLS/XLSM/PPT/PPTM: 실제 Office 변환 성공 및 입력 SHA-256 보존. 문서 생성 포함 10.69/9.19/7.52/5.64/3.72/0.62초.
- `tests/ocr_smoke.py`: 글자층 없는 한글 스캔 이미지 → Windows OCR → 텍스트 및 페이지 출처를 붙인 발췌 요약 통과.
- 2026-09-12 `tests/hwp_cancel_smoke.py`: 의도적으로 대기하는 도우미로 취소/시간 초과를 재현. 부모 프로세스에서 도우미 종료, 접근 허용 파일과 임시 레지스트리 값 제거, 원본 보존을 확인했다. 실제 한글 COM 무응답 자체를 재현한 검사는 아니다.
- 배포본에 포함된 OCR 실행 파일로 한글 스캔 PNG를 읽어 3문장 인식 성공.
- 2026-09-12 수정본: 탐색기에서 실행한 HWP/HWPX 회귀 검사 성공(문서 생성 포함 4.28/3.89초).
- 최종 EXE: 메인 창을 닫은 상태에서 HWP 우클릭 변환 → 빠른 창 완료 1/실패 0 → PDF 본문 `EoingPDF Hancom conversion test` 확인. 같은 HWP 우클릭 요약 → `sample_요약.txt`에 출처 파일과 `[p.1]` 본문 확인.
- 메뉴 등록/해제/재등록을 배포 앱에서 실행. 이전 메뉴 설정은 `LocalAppData/EoingPDF/backups/legacy-menu-20260912-042942-776364.json`에 백업. 실제 HWP 전체 메뉴에서 새 3개 항목 확인.
- `tests/performance_smoke.py`: 텍스트 PDF 20개·200페이지 병합 엔진 0.115초. 모든 페이지 본문 순서 및 입력 해시 일치. 앱 실행, 스캔, Office 변환이 제외된 합성 입력 측정이며 일반 문서 성능 보장은 아니다.

## 미해결 / 미검증

- 개발 명령 실행 환경의 COM 등록/한글 모듈 등록은 실제 사용자 탐색기 실행과 결과가 달랐다. 실제 탐색기 경로에서는 성공을 확인했으며, 환경 차이의 정확한 원인은 단정하지 않는다.
- 현재 PC와 다른 Windows/Office/한컴 버전의 호환성은 추가 검증 대상이다. Windows 11 기본 축약 메뉴 통합은 후속 범위이며 현재는 더 많은 옵션 표시에서 작동한다.
- GitHub 저장소 삭제/재생성: 2026-09-12 권한 승인 후 완료. 기존 ID 1280267163 → 새 ID 1366623248, 비공개 설정 유지. 이전 이력 번들 검증 완료.
- 빠른 창을 작업 시작 전에 닫는 경우 실행하지 않도록 수정하고 UI 검사 통과. 병합 단계 취소도 실패 없이 취소 상태를 반환하도록 수정하고 회귀 검사 통과.

## 수정된 배포 문제

PyInstaller가 PATH의 제3자 `icuuc.dll`을 수집하여 QtCore 로딩에 실패했다.
Windows 10/11 기본 ICU를 사용하도록 패키징에서 잘못된 ICU를 제외했으며, 배포 EXE 실행으로 수정 확인했다.
실사용하지 않는 Qt 플러그인을 제외한 중간 빌드는 ZIP 약 52MiB다. 최종 해시/크기는 마지막 빌드 후 다시 기록한다.

## 완료 조건

모든 사용자 요구사항을 추적하고, 우클릭 변환/병합/요약과 HWP/Office 실제 경로가 정상 동작하며,
실패/중단 시 원본 보존, 최종 배포 파일 검증, GitHub 재구성이 완료될 때만 정식 완료로 보고한다.
# 2.1.1 종료 동작 보강

- 사용자 제보 당시 열린 PDF 창이 없어 동일 현상은 직접 재현하지 못함.
- viewer_smoke.py: 종료 버튼 실제 클릭, Esc/F5, 중복 슬라이드쇼 방지, 최대화 창 닫기 및 저장 확인 취소/버리기 각 1회 검증 통과.
- 기존 14개 엔진 테스트 통과. 기존 원본 보존·임시 삭제·저장 검증 유지.
# 2.1.2 아이콘·가독성·설치형 검증

- 제공 PNG 원본과 프로젝트 복사본의 바이트 일치. 앱/문서 ICO 각각 16, 24, 32, 48, 64, 128, 256px 포함.
- 실제 Windows 뷰어에서 새 앱 아이콘과 흰 배경 확대 목록 확인. Pretendard 버튼 표시 확인.
- tests/installer_smoke.ps1 통과: 설치, 동일 경로 재설치, PDF 실행/아이콘 등록, 설치된 뷰어 실행, 제거, 사용자 파일 보존, 기존 포터블 연결 복원.
- PDF 기본 앱은 EoingPDF.Document 유지. 설치 EXE는 코드 서명 인증서 미적용.
- 기존 엔진 14개 테스트 및 viewer_smoke.py 통과.
- 2026-09-14: `pytest --collect-only` 기준 219개 테스트를 15개 파일 묶음으로 실행해 102 + 68 + 49 = 219개 전부 통과. `py_compile` 전체 통과, UI 회귀·클립보드·비교·발표자·설정·라이선스·양식·암호 뷰어·도장 smoke 통과.
- 2026-09-14: winget 제출 템플릿(`packaging/winget/manifest.yaml`)과 제출 절차 문서를 추가하고 배포 정적 게이트를 통과시켰습니다. 실제 제출 전에는 릴리스 URL과 SHA-256을 채워야 합니다.
- 2026-09-14: `scripts/package_source.py`와 `source_package_smoke.py` 재실행. `release/EoingPDF-2.2.0-source.zip` 157개 항목 CRC 및 민감한 실행 산출물 제외 검사 통과.
- 2026-09-14: 최신 소스 기준 테스트는 5개 파일 묶음 실행으로 안정화했습니다. 현재 223개이며 Qt·Windows 네이티브 종료 영향을 줄이기 위해 작은 묶음으로 나눠 전부 통과시킵니다. UI smoke, HWP/OCR 네이티브 빌드, PyInstaller frozen smoke, Inno Setup 컴파일은 배포 스크립트에서 이어집니다.
- 2026-09-14: `release/`에는 2.2.0 설치파일·포터블 ZIP·소스 ZIP만 남기고 2.0/2.1.2 산출물과 오래된 ShellBridge/해시 파일을 삭제했습니다. `build/previous-release-*` 임시 릴리스 폴더도 제거했습니다.
- 2026-09-14: 설치 smoke에서 격리 DPAPI 라이선스 시드 단계를 추가했습니다. 설치·업그레이드 로그와 제거 경계까지 실행했지만, 포터블 연결 복원 프로세스가 종료되지 않아 전체 smoke는 통과로 기록하지 않았습니다. 테스트 폴더와 사용자 설치 등록은 정리했습니다.
- 2026-09-14: 발표자 화면에 `P` 판서 모드와 노란 잉크 오버레이를 추가하고, 현재 페이지의 선을 PDF `Ink` 주석으로 새 파일에 저장하는 경로를 검증했습니다. `test_presenter_ink.py` 통과. Direct2D 120fps와 전체 발표 세션 벡터 합성은 별도 미완료입니다.
