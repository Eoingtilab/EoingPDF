# 어잉PDF 2.2.0 품질 검증 보고서

## 최신 후속 빌드 검증 — 2026-09-18 00시대

- 설치/폴더형 ZIP: 528개 전체 자동 검사, 75.99초. 정적 검사 0건. Windows UI, PDF/OCR/검색, 전체 스냅샷 복원 검사 통과. `temp/release-build-dock-20260917.log`.
- 추가된 드롭존: 실제 병합 페이지 순서, 여러 폴더/중복/누락/최대 개수, 출력 충돌/원본 보존, 실행 중 취소와 닫기, 화면 경계, 3언어×2테마 Windows UI 검사 통과. 목록의 부모 역참조로 생긴 Qt 접근 위반을 수정하고 기존 메인 창 생성과 함께 회귀 검사했다.
- 단일 EXE: 이름을 변경한 파일 하나에서 포함 폰트/PDF/ONNX 모델, 변환, 오프라인 뷰어/드롭존, 색인/검색 검사 통과. 실제 클래식 셸 등록·재등록·해제와 기존 프로젝트 등록 값 복원 통과. `temp/portable-build-dock-20260917.log`, `temp/portable-shell-smoke-dock-20260918.log`.
- 네이티브 셸: 클래식 Drop과 현대식 Invoke 각각 3작업, 총 6개 통과. 실제 선택 목록과 Python 작업 엔진을 사용하고 프로세스 시작은 기록용 EXE로 검사했다. 서명된 등록/탐색기 노출의 증거는 아니다.
- 최신 실제 설치·재설치·클래식 셸·뷰어·제거·사용자 파일 보존 통과. `/S` 배포 스크립트를 통한 실제 동일 경로도 통과. 추가 스크립트 검사 4개와 기존 드라이버/파이프라인 4개 통과. 설치 EXE 자체는 표준 Inno 옵션을 사용한다.
- 크기/체크섬은 `release/VALIDATION-20260917.md`와 `release/SHA256SUMS.txt`에 기록한다. 문서는 바이너리 빌드 후 갱신되므로 최종 소스 ZIP만 다시 생성한다. 실행 코드와 리소스의 빌드 이후 변경은 없다.
- 미서명 개발 통합 빌드다. 단일 EXE 자동 교체·복원, EDD 서버 교차 버전 업데이트, HWP 실제 모아찍기, QuickLook/성능, 전체 번역/소재/하드웨어 요구 등은 미완료이며 전체 목표는 계속 유지한다.

아래는 이전 빌드 시점의 기록이다.

## 최신 후속 빌드 검증 — 2026-09-17 12:26 KST

- 전체 자동 검사 510개 통과, 82.38초. 정적 검사 0건. 로그: `temp/release-build-continuation-final-20260917.log`.
- 슬라이드쇼 고정 청중 스타일·Quick 3언어·Windows DWM 프레임/단색 폴백·업데이트 상태 파일 공유 잠금·탐색기 DLL 전체 복원 보완을 포함한다.
- Windows UI 및 frozen PDF 도구/ONNX 검색/업데이트 복원 통과. frozen 복원은 실제 탐색기 DLL SHA-256 복원까지 검사한다.
- Qt 프로세스 배율 100/125/150/175/200% × 3언어 × 2테마 × 뷰어/설정 = 60조합 검사 통과. 물리 모니터 간 이동 검증은 아니다.
- 실제 설치→같은 버전 재설치→셸 등록→설치본 뷰어→제거→사용자 파일 보존 통과. 로그: `temp/installer-smoke-continuation-20260917.log`.
- 설치 파일 120,467,900 bytes, 포터블 ZIP 162,723,512 bytes. 정확한 최신 해시는 `release/SHA256SUMS.txt`와 `release/VALIDATION-20260917.md` 기준이다.
- Authenticode 상태 `NotSigned`. 현대식 셸 MSIX는 unsigned이며 등록하지 않았다. EDD 실제 교차 버전 업데이트·HWP RegisterModule 실패·검색/진단 성능·전체 번역/소재·하드웨어 수락은 남아 있다.
- 이 성공 기록은 설치/포터블 생성 후 작성했다. 최종 기록을 반영해 소스 ZIP만 다시 생성하며, 실행 코드와 네이티브 바이너리는 검증한 빌드와 동일하다.

이 아래는 이전 시점 기록이다. 전체 MASTER SPEC 수락 상태는 `MASTER_SPEC_ACCEPTANCE_20260917.md`에서 추적한다.

갱신일: 2026-09-15  
기준: ISO/IEC/IEEE 29119 테스트 계획·설계·실행·결함 증적 구조, ISO/IEC 25010 품질 모델  
범위: 현재 작업 폴더의 소스와 자동 테스트. 국제표준 인증서가 아니며 전체 조항 준수를 주장하지 않는다.

## 범위와 추적

요구사항 원본은 `docs/MASTER_SPEC_V2.md`, 구현 상태는 `docs/EXTENSION_STATUS.md`, 테스트 계획은 `docs/QUALITY_PLAN.md`, 실행 증적은 `docs/VALIDATION.md`에서 관리한다. 원본 PDF를 덮어쓰지 않고, 변환은 별도 결과 파일과 격리 프로세스를 사용한다.

## 테스트 실행

| 구분 | 증적 | 결과 |
|---|---|---|
| 단위·통합 테스트 | `python -m pytest tests -q -p no:cacheprovider --junitxml=temp/test-results/integration-latest.xml` | 381개 통과, 67.50초 (`temp/test-results/release-tests.xml`, 2026-09-15 전체 빌드 실행). 수집 시 생성되던 QCoreApplication과 이후 QWidget의 충돌을 공통 QApplication으로 수정했다. 네이티브 PDF 장시간 처리 문제라는 이전 추정은 이번 종료 원인의 근거가 아니었다. |
| Windows UI smoke | `ui_regressions.py`, `viewer_smoke.py`, `presenter_ui_smoke.py`, `native_ink_ui_smoke.py`, `form_ui_smoke.py`, `settings_ui_smoke.py` | 2026-09-15 `QT_QPA_PLATFORM=windows`에서 6개 스크립트 통과. 합성 입력 기반이며 물리 펜/실제 다중 모니터 사용자 시험을 대신하지 않는다. |
| 격리 변환 | `encrypted_viewer_smoke.py`, `frozen_smoke.py` | 암호·취소·별도 프로세스 경로 검증. 2026-09-15 재빌드의 frozen smoke에서 SVG·DXF·윤곽선·Markdown·워터마크·보정·OCR·소책자 8개 처리 통과. 별도 frozen_update_smoke에서 실제 백업 실행본의 종료 대기·복원·복원 후 PDF 처리 통과. |
| 보안 경계 | `test_licensing.py`, `test_updates.py`, `test_seal_vault.py`, `test_sanitize.py` | DPAPI·해시·원본 보존·경로 검증 검사 |
| 패키징 | `build_release.ps1`, Inno Setup, `tests/installer_smoke.ps1` | 2026-09-15 PyInstaller·Inno 컴파일 완료. installer_smoke 실설치·같은 버전 재설치·셸/아이콘 등록·설치 뷰어 실행·제거·사용자 파일 보존 통과. 처음 sandbox 실행은 접근 거부로 롤백됐고, 권한 범위를 확대한 동일 시험은 통과했다. 다른 버전 간 업데이트와 코드 서명은 남아 있다. |
| 소스 배포 | `scripts/package_source.py`, `tests/source_package_smoke.py` | 2026-09-15 빌드 시 201개 명시 항목 CRC·필수 모듈 포함·개인 문서/실행 바이너리 제외 검사 통과 |

## ISO/IEC 25010 현재 평가

| 특성 | 확인된 내용 | 남은 증적 |
|---|---|---|
| 기능 적합성 | PDF 보기·삭제·병합·변환·OCR·암호·양식·발표·고급 도구와 우클릭 흐름 | 로컬 RAG 구현·DXF 실제 CAD 호환성, Direct2D 실화면 4K/120fps·윤곽선 PDF의 실인쇄 호환성 등 |
| 성능 효율성 | 엔진 내부 진단 중앙값과 변환 메모리 제한을 측정 | 새 프로세스 포함 D-01~D-10 30ms 목표, 실제 대형 문서·듀얼 4K |
| 호환성 | Office/HWP 경로와 Windows OCR, PDF 암호·회전·CropBox 테스트 | LibreOffice 실설치, 다양한 실제 문서·Office 붙여넣기 |
| 사용성 | Pretendard, 한글 UI, 썸네일·키보드·확인창·오프라인 라이선스 | 전체 화면의 완전한 다국어, 실제 사용자 평가 |
| 신뢰성 | 원본 해시 보존, 취소·임시파일 정리, 손상 복구 경계 | 설치 실패를 유도한 자동 롤백 실검증 |
| 보안성 | EDD 키 DPAPI, 업데이트 SHA-256/HTTPS, 암호 stdin 격리, redaction | 코드 서명, 외부 침투 시험, 모든 포렌식 흔적 제거 |
| 유지보수성 | 모듈별 테스트·문서·명시적 소스 패키지 | SDK 공통 화면 전체 통합 |
| 이식성 | Windows 10/11 x64 설치·포터블 구조 | winget·SignPath·Windows 11 IExplorerCommand |

## 결함 처리 규칙

원본 훼손, 암호 우회, 결과 해시 불일치, 설치파일 서명/해시 불일치, 사용자 파일 삭제는 배포 차단 결함이다. OCR 누락, 복잡한 읽기 순서, 이미지 내부 개인정보는 결과 검토가 필요한 제한사항으로 UI와 README에 고지한다. 이 문서에 없는 “통과”는 주장하지 않는다.

## 정식 출시 판정

현재는 개발 중이며 정식 출시 조건을 충족하지 않았다. 2026-09-15 빌드에 최근 기능과 업데이트 도우미를 포함했고 설치·재설치·제거를 확인했다. 실제 EDD 인증/버전 간 업데이트·설치 실패 자동 복구의 전체 경로, 서명, 잔여 요구사항이 아직 남아 있다.

전체 검사 통과는 현재 구현된 테스트 범위의 결과다. 미구현 요구사항을 완료로 판정하는 근거로 사용하지 않는다. `tests/conftest.py`가 수집 전에 GUI 지원 Qt 앱을 유지하며, 배포 스크립트도 묶음 실행 대신 전체 테스트와 JUnit XML 출력을 사용한다. 전체 빌드 후 frozen 복구 검사를 추가로 통과했으며, 향후 빌드에서는 새 staging 실행본에 대해 이 검사도 필수 실행하도록 연결했다.


## 2026-09-15 통합 빌드 식별

- 설치형: `EoingPDF-2.2.0-Setup-x64.exe`, 82,570,423 bytes, SHA-256 `E663492A9F91C75BBCC384BD634FCA88B5527A8661EF5CA48094A09D33DA8581`.
- 포터블: `EoingPDF-2.2.0-portable.zip`, 117,505,327 bytes, SHA-256 `33D8A9808D77D481A18A28E42CD4CF37426A414BE953B0C75488E6DBD79274BE`.
- 위 산출물은 이번 보고 갱신 직전 빌드다. 갱신된 보고와 향후 frozen 복구 검사 단계 추가는 다음 배포 문서에 반영한다.
- 설치 로그: `temp/installer-smoke/install-1.log`, `install-2.log`. 사용자 실제 라이선스를 사용하지 않은 격리 시험이며 서버 인증 성공을 증명하지 않는다.


## 최신 통합 재검증

2026-09-15 최신 빌드는 휠 페이지 이동, 추천 파일명 사본 저장, 진단 더 보기, 임시 판서 메모리 회수, 업데이트 준비 작업 분리와 실행 검사를 포함한다. 전체 자동 검사 381개, Windows UI 검사, frozen 실행 검사·8개 변환·복구 도우미 검사를 통과했다. 새 설치파일로 격리 설치·같은 버전 재설치·등록·뷰어·제거·사용자 파일 보존 검사를 다시 통과했다. 로그의 upgrade는 같은 버전 재설치를 뜻하며 실제 교차 버전 업데이트 완료를 의미하지 않는다. 현재 소스 보고서는 빌드 후 검사 결과로 갱신했으므로 패키지 내부 보고서는 이 갱신 전이다.


## 2026-09-16 검색 통합 빌드 검사

현재 빌드의 전체 자동 테스트는 428개 통과(81.02초, temp/test-results/release-tests.xml). Windows 실제 UI 검사에서 너비 맞춤, 표 복사, 비교, 발표자·그리드·단계 노출, Direct2D 입력과 한글 IME, 설정·라이선스, 양식, 암호 뷰어, 도장 흐름을 통과했다.

이번 소스는 폴더 검색, 두 단계 색인 생성, 취소·충돌 시 기존 색인 보존, 검색 자료 삭제, Windows OCR 혼합 페이지 검색, 오래된 결과 제외를 포함한다. 검색 자식 프로세스의 제한된 텍스트 표본 측정은 최대 약 85MB였으며 UI와 OCR 보조 프로세스는 별도다. 모델의 검색 정확도는 평가 중이다. 이 기록 시점에 실행파일 패키징이 진행 중이므로 새 frozen/설치본 검사는 아직 완료로 판정하지 않는다. HWP 모아찍기 초기화는 단위 검사만 통과했고 실제 한글 접근 모듈 등록 실패가 남아 있다.

### 2026-09-16 빌드 완료 및 설치 검증

위 진행 중 기록 이후 PyInstaller·Inno 빌드를 완료했다. 새 frozen 실행본의 뷰어/폰트 검사, 8개 변환, 포함된 ONNX 모델의 일반·스캔 PDF 검색과 두 단계 색인 저장, 백업 실행본의 업데이트 복구가 통과했다. 소스 ZIP은 229개 항목으로 필수 모델 파일과 출처 고지 포함 및 CRC 검사를 통과했다.

새 설치파일로 실제 설치·같은 버전 재설치·셸 등록·설치 뷰어·제거·사용자 파일 보존 검사를 통과했다. 여기의 재설치는 실제 교차 버전 자동 업데이트나 EDD 인증을 검증한 것이 아니다.

- 설치 EXE: 120,368,488 bytes, SHA-256 BD1D96B2ADFC3BDEC7D7E6530A772A0E3FF6BBBE4BFB34F8BDAC729D55EBF0D3.
- 포터블 ZIP: 162,572,219 bytes, SHA-256 E1492BCFA5C8AE5FD0B85CB65FC130FEE2848AF050457A1A295B74E611E3B66D.

이 문단은 빌드 완료 후 갱신해 패키지 내부 문서에는 아직 포함되지 않는다. 정식 출시의 남은 조건은 앞의 정식 출시 판정과 요구사항 추적표를 따른다.


### 2026-09-16 추가 소스 검증: 다국어 도구 및 한글 검사 제한 시간

- 메인 창의 9개 도구 제목·설명·실행 버튼, 페이지 범위 안내 및 파일 수를 언어 리소스에 연결했습니다. 한국어·영어·일본어에서 도구 선택, 파일 추가, 실행 버튼 활성 상태 및 뷰어 동작을 검사했습니다.
- 실제 한글 검사에 형식별 180초 제한 시간을 추가했습니다. 검사 자식 프로세스가 멈추면 실패 처리하고 부모가 파일 접근 허용을 회수합니다. 사용자 한글 프로세스는 종료하지 않습니다. COM 서버 종료 자체를 보장하는 검사는 아닙니다.
- 관련 회귀 검사 38개 통과. 실제 한글 호출 없이 검사 감독기의 정상 종료·실패·시간 초과 경로를 검증했습니다.
- 사용자가 한글을 저장하고 종료했다고 확인했으나 실제 변환 재검증은 자동 승인 검토의 사용량 한도 초과로 실행되지 않았습니다. HWP/HWPX 모아찍기 설정 독립성은 실환경 재검증이 남아 있습니다. 이 변경은 기존 설치 파일에 아직 포함되지 않았습니다.


### 2026-09-16 캡처 수집 통합 검사

캡처 수집·검토·PDF 저장 기능을 추가한 소스에서 전체 자동 검사 446개가 73.57초에 통과했습니다. 이 검사는 Qt offscreen 플랫폼을 사용하며 실제 Windows 캡처 키/클립보드 소유권/설치본 검사를 대체하지 않습니다. 이후 진단 코드의 D-08/D-10 오분류와 단일 페이지에 무의미한 세로 연결 추천을 수정하고 전체 비우기 검사를 추가해 관련 13개 검사가 통과했습니다. 최신 후속 수정까지의 전체 재실행 결과로 446개를 해석하지 않습니다.

캡처 데이터는 최대 16개·32MB로 제한하고 마지막 앱 창을 닫거나 파괴하면 해제합니다. 실제 PDF 생성 후 페이지 수·순서·색상·크기를 읽어 검증했으며, 저장 실패 시 임시 파일 정리와 기존 파일 보존도 검사했습니다. PDF 내부 이미지 진단은 CONTENT-IMAGES, 잠긴/손상된 PDF는 PDF-LOCKED/PDF-DAMAGED로 구분하여 명세의 실제 캡처 수집 및 전후 비교 요구를 달성한 것처럼 표시하지 않습니다.


### 2026-09-16 증빙 통합 및 이미지 선택 수정

safe_submission 통합 작업에 대해 관련 자동 검사 38개 통과(3.07초), 실제 Windows 비교 UI 검사 통과. Windows OCR 통합 검사는 이미지 속 합성 전화번호가 입력에서 인식되고 출력에서는 제거되는지 및 원본 바이트 보존을 확인한다. 별도 픽셀 검사는 검은 오버레이만 존재하는 것이 아니라 내부 이미지의 원래 색 픽셀이 제거되는 것을 확인한다. OCR 전체 정확도·실제 신분증 제출 적합성 증거로 확대 해석하지 않는다.

이미지 교체 선택 창의 NameError 및 선행 선택 검사 위치를 수정하고 관련 12개 검사 통과(1.82초). 이 후속 변경 전체에 대한 통합 빌드 및 설치본 검사는 아직 수행하지 않았다.


### 2026-09-17 빌드 재개와 진단 검사 수명 정리

이전 빌드 세션 핸들은 소멸했고 실행 중 빌드 프로세스도 없으며 설치 산출물은 9월 16일 07:10의 이전 파일임을 확인했다. 새 빌드는 전체 자동 검사 중 test_sniffer_lifecycle의 지연 삭제 처리에서 Python Aborted로 중단했다. 새 실제 print 진단 검사가 소유하던 부모 없는 MicroSniffer를 종료 후 명시적으로 deleteLater하고 같은 검사 안에서 지연 삭제를 처리하도록 수정했다. 단독 및 선행 인쇄/후행 증빙 검사를 포함한 12개 검사를 별도 프로세스로 3회 실행해 모두 통과했다. 제품 코드의 임의 오류를 숨기거나 검사를 제거하지 않았다. 설치 빌드는 재진행 중이며 성공 전까지 기존 산출물을 최신으로 표시하지 않는다.

한글이 실행 중이지 않은 상태에서 승인된 실제 HWP 검사를 다시 실행했다. 이번에는 승인 검토 제한이 아니라 RegisterModule=False로 종료되었다. 합성 문서 생성·인쇄 단계에 도달하지 못했으므로 모아찍기 수정의 실환경 성공으로 간주하지 않는다. 32비트 별도 로더로 현재 hwp-x86.dll의 LoadLibrary와 IsAccessiblePath export 조회는 성공했고 검사 후 한글 프로세스는 남지 않았다.


### 2026-09-17 통합 설치 빌드 완료

- 전체 자동 검사: 459개 통과, 60.85초. JUnit: temp/test-results/release-tests.xml.
- Windows UI 검사: 너비 맞춤/페이지 이동/한국어 버튼/클립보드 선택/비교/발표/Direct2D 판서/IME/설정/라이선스/양식/암호/도장 통과.
- 패키징된 실행본: 뷰어 및 폰트 실행 검사, SVG/DXF/윤곽선/Markdown/워터마크/기울기/증빙 통합/밝은 인쇄 사본/OCR/소책자, ONNX 검색 및 업데이트 파일 복원 통과.
- 설치 검사: 별도 테스트 폴더 설치, 같은 버전 재설치, 셸 등록, 설치본 뷰어, 제거, 사용자 파일 보존 통과. 실제 교차 버전 업데이트 검증은 아님.
- 소스 ZIP: 240개 항목 검증 통과.
- 빌드 로그: temp/release-build-20260917-retry.log. 설치 로그: temp/installer-smoke-20260917.log.

산출물:

| 파일 | 바이트 | SHA-256 |
|---|---:|---|
| EoingPDF-2.2.0-Setup-x64.exe | 120401400 | 5AD3746867FE2DC858948035518C3D1A06F8A45769EBB6EC823F64D70E9F4776 |
| EoingPDF-2.2.0-portable.zip | 162611674 | 94A4721B82A00F3B440D8EF63B392CBF2407C7C2B8BAC13D7BDD8BF8673592D4 |
| EoingPDF-2.2.0-source.zip | 30484004 | B86F199E46A3641E16FFB2202030357898031694F56BEFD22B2C1D276D450959 |

캡처 수집, 증빙 통합, 인쇄 준비, 최근 UI/다국어 및 업데이트 상태 수정은 이 설치본에 포함되었다. 이 기록은 산출물 생성 후 작성했으므로 압축 파일 내부 문서에는 이후 성공 기록이 들어 있지 않다. 설치본에 포함된 README의 일부 '다음 설치 빌드' 표기는 개발 중 문구이며 위 기능의 미포함을 뜻하지 않는다. 최종 정식 문서 정리 대상이다.

이전 백업 build/previous-release-20260916-070956를 경로·재분석 지점·실행 프로세스 확인 후 삭제했다. 276418596 bytes 제거, 직전 정상 백업 build/previous-release-20260917-052558는 유지했다. 프로젝트 전체 정리 완료를 뜻하지 않는다.

한글 RegisterModule 실환경 실패, 실제 교차 버전 서버 업데이트, Windows 11 현대식 셸, 전체 SDK 다크/반투명 UI 및 명세 전체 성능/하드웨어 검증 등은 미완료다. 본 빌드는 통합 개발 빌드이며 전체 목표 달성/표준 인증을 선언하지 않는다.


### 2026-09-17 정적 코드 검사 및 불필요한 가져오기 정리

개발 전용 pyflakes 3.4.0을 고정하고 src, main.py, scripts를 검사했다. 제품 코드에서 미정의 이름은 보고되지 않았고, app/quick/updates/viewer의 미사용 import 5개를 제거한 뒤 정적 검사 결과 0건을 확인했다. Qt 객체를 유지하는 테스트의 지역 참조는 단순 미사용 표시만으로 제거하지 않았다. 빌드 시작 시 정적 검사를 필수 실행하도록 연결했다. 이 검사는 실행/통합 검사를 대체하지 않는다. 의존성은 requirements-dev.txt에만 추가하며 제품 런타임 의존성을 늘리지 않는다.

소스 README에서 캡처 수집과 증빙 통합의 임시 ‘다음 설치 빌드’ 표기를 실제 포함된 2026-09-17 통합 빌드로 갱신했다. 기존 설치 산출물은 다시 만들지 않았으며 다음 정식 빌드에서 문서와 정적 검사 변경을 포함해야 한다.


## 변환 결과의 원자적 저장 (2026-09-17, 설치 빌드 이후)

공통 to_pdf는 대상 폴더 안의 작업 전용 임시 경로로 변환하고 PDF 유효성과 취소 상태를 확인한 뒤 새 파일로만 게시한다. 기존 원본/대상 파일은 변환 전에 거부하고, 변환 중 다른 프로세스가 같은 이름의 파일을 만들어도 덮어쓰지 않는다. PDF·텍스트·이미지·Office·한글 경로에 적용된다. 실패한 native helper의 부분 출력은 전용 임시 경로에서만 제거하고 LibreOffice 대체 변환을 시작한다.

LibreOffice 단독 변환도 완성된 출력물을 대상 볼륨의 임시 경로로 복사한 뒤 게시한다. 복사 중 디스크 오류와 복사 직후 취소는 최종 파일을 남기지 않으며, 같은 이름의 새 파일은 보존한다. 개인 LibreOffice 프로필을 변경하지 않는 기존 동작을 유지한다.

관련 26개 검사 통과(1.01초). 기존 엔진 검사와 함께 실제 PDF 생성/읽기, 취소·실패·파일 충돌·부분 native 출력 후 fallback·복사 오류 경로를 검증했다. 외부 Office/LibreOffice 실행 자체는 이 회귀 검사에서 대역으로 제어했으므로 실제 문서 형식 호환성이나 한글 모듈 실패 해결을 입증하지 않는다. src/main.py/scripts 정적 검사도 0건이다. 변경은 2026-09-17 설치본 이후 소스에 있으며 다음 빌드 대상이다.


## 현대식 메뉴 패키지·공통 번역 검증 (2026-09-17, 설치 빌드 이후)

Windows SDK MakeAppx로 서명 전 sparse 앱 ID 패키지를 생성했다. 외부 EXE/DLL 참조, COM 3개와 메뉴 verb 연결, 아이콘 크기 및 잘못된 식별값 거부를 검사했다. 패키지 생성은 인증서 설치나 사용자 계정 등록을 수행하지 않는다. 신뢰할 수 있는 코드 서명과 실제 Windows 11 탐색기 노출 검증은 남아 있다.

네이티브 메뉴 제목은 assets/locales의 ko-KR/en-US/ja-JP에서 빌드 시 생성한다. 고정 UTF-16 이스케이프를 사용하여 따옴표·역슬래시·줄바꿈·보조 평면 문자도 안전하게 컴파일하며, 잘못된 번역은 기존 헤더를 교체하기 전에 실패한다. DLL을 다시 빌드하고 실제 COM 제목이 OS 언어의 JSON 문구와 일치하는지 검사했다.

전체 회귀 검사 최초 실행은 481개 통과/2개 실패였다. 이전 미사용 subprocess import 정리 이후 업데이트 검사 두 곳이 제거된 모듈 속성을 모킹하고 있었다. 실제 실행 경계인 update_helper.launch를 검사하도록 고쳤다. 작업 중 설치 방지 및 변조 파일 실행 차단의 검증 조건은 유지했다. 업데이트 관련 19개 검사 통과 후 전체 재실행 483개가 56.31초에 통과했다. JUnit: temp/test-results/source-shell-integration-20260917.xml. 정적 검사 결과 0건. 이 소스 변경과 현대식 셸 패키지는 기존 설치 산출물에 아직 포함하지 않았다.
